# Unraid server settings
# Considered headless because Unraid uses a custom Linux-based OS
# with a web-based GUI rather than traditional desktop environment
headless: true
terminalType: "xterm-256color"
