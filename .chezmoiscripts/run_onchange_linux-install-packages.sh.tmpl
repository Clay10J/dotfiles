{{- if eq .chezmoi.os "linux" -}}
#!/usr/bin/env sh
set -eu

# Refresh once
sudo apt-get update

# Install everything declared in .chezmoidata/packages.yaml
cat <<EOF | xargs -r sudo apt-get install -y
{{- range .packages.linux.apt }}
{{ . }}
{{- end }}
EOF
{{- end -}}
