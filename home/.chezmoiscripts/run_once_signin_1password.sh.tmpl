#!/usr/bin/env bash
set -e

# Only run once, after the CLI has been installed
if ! command -v op >/dev/null 2>&1; then
  echo "⚠️  1Password CLI not found; skipping sign-in"
  exit 0
fi

eval "$(op signin)"
